% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jgd.R
\name{jgd}
\alias{jgd}
\title{JSON Graphics Device}
\usage{
jgd(width = 8, height = 6, dpi = 96, socket = NULL)
}
\arguments{
\item{width}{Device width in inches (default 8).}

\item{height}{Device height in inches (default 6).}

\item{dpi}{Resolution in dots per inch (default 96).}

\item{socket}{Socket address for the rendering server. Supports URI formats
(\verb{tcp://host:port}, \verb{unix:///path/to/socket}) or raw Unix socket paths.
If \code{NULL} (default), use the \code{jgd.socket} R option, falling back to the
\code{JGD_SOCKET}environment variable. If \code{JGD_SOCKET} environment variable is
also unset, the device discovers the socket via the discovery file.}
}
\value{
Invisible \code{NULL}. The device is opened as a side effect.
}
\description{
Opens a graphics device that streams plot operations as JSON to an external
renderer (e.g. VS Code extension or browser) over a Unix domain socket.
}
\section{Displaying plots with \code{jgd}}{

It is important to note that \code{jgd()} does not display any plots; it only
streams them (i.e., converts them to a format that a JSON renderer
understands). To actually \emph{display} your plots with \code{jgd}, you'll need an
appropriate frontend. Two official renderers are available:
\itemize{
\item \strong{VS Code.} Native \code{jgd} support is built into the VS Code R extension
(\url{https://github.com/REditorSupport/vscode-R}), which handles device
activation for you.
\item \strong{Deno server.} A standalone browser-based renderer, available from the
project repository: \url{https://github.com/grantmcdermott/jgd}.
}

Users aren't limited to these two options. The \code{jgd} protocol is deliberately
frontend-agnostic; you can render plots with any client that reads JSONL
(JSON Lines). Again, please see the project repository for full documentation:
\url{https://github.com/grantmcdermott/jgd}
}

\section{Debugging}{

Set \code{options(jgd.debug = TRUE)} before opening the device to enable
frame-level diagnostic output on stderr (via \code{REprintf}).  This logs
details about \code{newPage}, \code{flush_frame}, and \code{poll_resize} events, which
is useful for diagnosing resize/replay issues.
}

\section{Protocol specification}{

The jgd protocol is a simple, versioned JSONL wire format designed to be
frontend-agnostic. You can use it to build your own renderer (e.g., for
Neovim, Emacs, or a custom web app). See \code{\link{jgd_spec}} for the complete
specification covering transports, message schemas, drawing operations,
the resize protocol, font metrics, and multi-session routing.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Requires a running renderer (e.g., VS Code extension or Deno server).
# See the "Displaying plots" section above.
library(jgd)
jgd()
plot(1:10)
lines(1:10, col = "red", lwd = 3)
hist(rnorm(1000), col = "steelblue")
dev.off()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{jgd_spec}} for the protocol specification;
\code{\link[=jgd_ext]{jgd_ext()}} and \code{\link[=with_jgd_ext]{with_jgd_ext()}} for renderer extensions.
}
